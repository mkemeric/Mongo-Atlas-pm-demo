# ============================================================================
# MongoDB Atlas Configuration
# ============================================================================
# Get your connection string from MongoDB Atlas:
# 1. Go to your cluster
# 2. Click "Connect" → "Connect your application"
# 3. Copy the connection string and replace <password> with your database password

MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?appName=myApp
DATABASE_NAME=pmd

# ============================================================================
# HPE MLIS LLM Configuration (for answer generation)
# ============================================================================
# Endpoint format: https://<model-name>.<namespace>.<cluster-domain>/v1
# Get your token from MLIS administrator or via kubectl

LLM_API_ENDPOINT=https://llama-3-1-8b-instruct-1.your-domain.com/v1
LLM_API_KEY=your-kubernetes-service-account-token
COMPLETION_MODEL=meta/llama-3.1-8b-instruct

# ============================================================================
# HPE MLIS Embedding Configuration
# ============================================================================

# Set to 'true' for HPE MLIS (recommended)
USE_OPENAI_COMPATIBLE_EMBEDDINGS=true

# HPE MLIS Embedding Endpoint
EMBEDDING_API_ENDPOINT=https://embedqa-e5-v5.your-domain.com/v1
EMBEDDING_API_KEY=your-kubernetes-service-account-token
EMBEDDING_MODEL=nvidia/nv-embedqa-e5-v5

# ============================================================================
# SSL Configuration
# ============================================================================
# Set to 'true' for internal deployments with self-signed certificates
# Set to 'false' for production with proper SSL certificates

DISABLE_SSL_VERIFICATION=true

# ============================================================================
# Legacy Voyage AI Configuration (Optional)
# ============================================================================
# Only needed if using Voyage AI's cloud service instead of MLIS
# Uncomment these lines and set USE_OPENAI_COMPATIBLE_EMBEDDINGS=false

# VOYAGE_API_ENDPOINT=https://api.voyageai.com/v1
# VOYAGE_API_KEY=pa-r-your-voyage-api-key
# EMBEDDING_MODEL=voyage-3-large

# ============================================================================
# Configuration Examples
# ============================================================================

# Example 1: HPE MLIS with NVIDIA Embeddings + Meta Llama (Recommended)
# USE_OPENAI_COMPATIBLE_EMBEDDINGS=true
# EMBEDDING_API_ENDPOINT=https://embedqa-e5-v5.domain/v1
# EMBEDDING_API_KEY=your-mlis-token
# EMBEDDING_MODEL=nvidia/nv-embedqa-e5-v5
# LLM_API_ENDPOINT=https://llama-3-1-8b-instruct-1.domain/v1
# LLM_API_KEY=your-mlis-token
# COMPLETION_MODEL=meta/llama-3.1-8b-instruct
# DISABLE_SSL_VERIFICATION=true

# Example 2: HPE MLIS with Voyage Embeddings
# USE_OPENAI_COMPATIBLE_EMBEDDINGS=true
# EMBEDDING_API_ENDPOINT=https://voyage-embed.domain/v1
# EMBEDDING_API_KEY=your-mlis-token
# EMBEDDING_MODEL=voyage-3-large

# Example 3: Voyage AI Cloud Service (Alternative)
# USE_OPENAI_COMPATIBLE_EMBEDDINGS=false
# VOYAGE_API_KEY=pa-r-your-voyage-cloud-key
# EMBEDDING_MODEL=voyage-3-large

# ============================================================================
# Important Notes
# ============================================================================
# - Do NOT use quotes around values in .env files
# - MLIS tokens expire - check expiration and regenerate as needed
# - Model names must match exactly what's deployed on your MLIS instance
# - Endpoint URLs must include /v1 path
# - For testing: Use DISABLE_SSL_VERIFICATION=true
# - For production: Get proper SSL certificates and set DISABLE_SSL_VERIFICATION=false
